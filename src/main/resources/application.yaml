spring:
  datasource:
    username: media
    password: media
    url: jdbc:mysql://localhost:3306/media
  
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    defer-datasource-initialization: true  
  
  sql:
    init: 
      mode: 

springdoc:
  swagger-ui:
    path: /